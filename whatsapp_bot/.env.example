# Where the FastAPI backend is reachable from the machine running this bot.
# It must be reachable by the bot process, and (for image sending) reachable by WhatsApp Web.
BACKEND_URL=http://localhost:8000

# Required only when backend is running with HWEIBO_PROFILE=real.
# This must match backend's HWEIBO_API_KEY.
HWEIBO_API_KEY=

# Optional: reduce noise / tweak runtime.
HEADLESS=true
MAX_HISTORY=20

